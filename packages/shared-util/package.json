{
  "name": "@corbado/shared-util",
  "version": "0.1.0",
  "description": "Shared utilities for Corbado packages",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "browser": "dist/index.browser.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc -b",
    "build:dev": "npm run build",
    "build:types": "tsc",
    "build:bundles": "esbuild src/index.ts --bundle --platform=node --target=node16 --outfile=dist/index.js && esbuild src/index.ts --bundle --platform=browser --target=es2018 --outfile=dist/index.browser.js && esbuild src/index.ts --bundle --format=esm --platform=node --target=node16 --outfile=dist/index.mjs",
    "clean": "rimraf dist",
    "prebuild": "npm run clean"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "corbado",
    "utilities"
  ],
  "author": "Corbado",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0",
    "esbuild": "^0.20.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
