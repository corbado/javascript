import {CommonScreens} from "../../utils/constants/flowHandler";

export const defaultTimeout = 30000;

export const flowStartScreen = CommonScreens.Start;
export const flowEndScreen = CommonScreens.End;
